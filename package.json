{
  "name": "codex-runtime-v0.1",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "pnpm --filter @codex/orchestrator start",
    "build": "pnpm -r build",
    "migrate": "node scripts/migrate.js",
    "ci": "./ci_v0.3.sh",
    "dev": "docker compose -f docker-compose.dev.yml up"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "node-fetch": "^3.3.2",
    "pg": "^8.16.3"
  }
}